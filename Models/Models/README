논문에 사용된 모형들 코드입니다.

현재 준비중이며, 
1. 논문에서 사용한 모형코드 
2. 직접 구현한 모형코드 

두가지 폴더로 구성되어있습니다.

[2. 제가 구현한 모형코드] 는 튜닝을 안한 상태이기때문에 결과는 크게 의미가 없으며,
아래의 몇가지 모형에 대해 제 논문에서 사용한 코드 중, 추가로 직접 수정해본 코드입니다.
--------------------------------------------------------------------------------------------------
ResNet18, ResNet50, Inception-ResNet-v2, ResNext50, 
SE-ResNext50, DenseNet121, SE-DenseNet121 (8개)
--------------------------------------------------------------------------------------------------

직접 구현한 모형코드는 Caltech 데이터 기준을 따릅니다.
